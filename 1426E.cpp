#include<bits/stdc++.h>
using namespace std;
long long n,a1,b1,c1,a2,b2,c2,k,min1=0,ans=LLONG_MAX,a,b,c,x,y,z;
int main()
{
    cin>>n;
    cin>>a1>>b1>>c1;
    cin>>a2>>b2>>c2;
    a=a1,b=b1,c=c1,x=a2,y=b2,z=c2;
    //min
    k=min(a1,a2);
    a1-=k,a2-=k;
    k=min(a1,c2);
    a1-=k,c2-=k;
    k=min(a1,b2);
    min1+=k;
    a1-=k,b2-=k;
    k=min(b1,b2);
    b1-=k;b2-=k;
    k=min(b1,a2);
    b1-=k;a2-=k;
    k=min(b1,c2);
    min1+=k;
    b1-=k,c2-=k;
    k=min(c1,c2);
    c1-=k,c2-=k;
    k=min(c1,b2);
    c1-=k,b2-=k;
    k=min(c1,a2);
    min1+=k;
    c1-=k,a2-=k;
    ans=min(ans,min1);min1=0;

    //min2
    a1=a,b1=b,c1=c,a2=x,b2=y,c2=z;
    k=min(a1,a2);
    a1-=k,a2-=k;
    k=min(a1,c2);
    a1-=k,c2-=k;
    k=min(a1,b2);
    min1+=k;
    a1-=k,b2-=k;
    k=min(c1,c2);
    c1-=k,c2-=k;
    k=min(c1,b2);
    c1-=k,b2-=k;
    k=min(c1,a2);
    min1+=k;
    c1-=k,a2-=k;
    k=min(b1,b2);
    b1-=k;b2-=k;
    k=min(b1,a2);
    b1-=k;a2-=k;
    k=min(b1,c2);
    min1+=k;
    b1-=k,c2-=k;

    ans=min(ans,min1);min1=0;
    //minb
    a1=a,b1=b,c1=c,a2=x,b2=y,c2=z;
    k=min(b1,b2);
    b1-=k;b2-=k;
    k=min(b1,a2);
    b1-=k;a2-=k;
    k=min(b1,c2);
    min1+=k;
    b1-=k,c2-=k;

    k=min(c1,c2);
    c1-=k,c2-=k;
    k=min(c1,b2);
    c1-=k,b2-=k;
    k=min(c1,a2);
    min1+=k;
    c1-=k,a2-=k;
    k=min(a1,a2);
    a1-=k,a2-=k;
    k=min(a1,c2);
    a1-=k,c2-=k;
    k=min(a1,b2);
    min1+=k;
    a1-=k,b2-=k;

    ans=min(ans,min1);min1=0;
    //minb
    a1=a,b1=b,c1=c,a2=x,b2=y,c2=z;
    k=min(b1,b2);
    b1-=k;b2-=k;
    k=min(b1,a2);
    b1-=k;a2-=k;
    k=min(b1,c2);
    min1+=k;
    b1-=k,c2-=k;
    k=min(a1,a2);
    a1-=k,a2-=k;
    k=min(a1,c2);
    a1-=k,c2-=k;
    k=min(a1,b2);
    min1+=k;
    a1-=k,b2-=k;
    k=min(c1,c2);
    c1-=k,c2-=k;
    k=min(c1,b2);
    c1-=k,b2-=k;
    k=min(c1,a2);
    min1+=k;
    c1-=k,a2-=k;

    ans=min(ans,min1);min1=0;
    //minc
    a1=a,b1=b,c1=c,a2=x,b2=y,c2=z;
    k=min(c1,c2);
    c1-=k,c2-=k;
    k=min(c1,b2);
    c1-=k,b2-=k;
    k=min(c1,a2);
    min1+=k;
    c1-=k,a2-=k;
    k=min(b1,b2);
    b1-=k;b2-=k;
    k=min(b1,a2);
    b1-=k;a2-=k;
    k=min(b1,c2);
    min1+=k;
    b1-=k,c2-=k;
    k=min(a1,a2);
    a1-=k,a2-=k;
    k=min(a1,c2);
    a1-=k,c2-=k;
    k=min(a1,b2);
    min1+=k;
    a1-=k,b2-=k;

    ans=min(ans,min1);min1=0;
    //minc
    a1=a,b1=b,c1=c,a2=x,b2=y,c2=z;
    k=min(c1,c2);
    c1-=k,c2-=k;
    k=min(c1,b2);
    c1-=k,b2-=k;
    k=min(c1,a2);
    min1+=k;
    c1-=k,a2-=k;
    k=min(a1,a2);
    a1-=k,a2-=k;
    k=min(a1,c2);
    a1-=k,c2-=k;
    k=min(a1,b2);
    min1+=k;
    a1-=k,b2-=k;
    k=min(b1,b2);
    b1-=k;b2-=k;
    k=min(b1,a2);
    b1-=k;a2-=k;
    k=min(b1,c2);
    min1+=k;
    b1-=k,c2-=k;
    ans=min(ans,min1);
    // max
    a1=a,b1=b,c1=c,a2=x,b2=y,c2=z;
    long long ans2=min(a1,b2)+min(b1,c2)+min(c1,a2);
    cout<<ans<<" "<<ans2<<endl;
}
