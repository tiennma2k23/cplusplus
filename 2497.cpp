#include<bits/stdc++.h>
using namespace std;
int main()
{
    long int n,kq,d=0;
while (true)
{
    cin>>n;
    if (n==-1) break;
    kq=n/2;
    if (n!=-1) d++;

    cout<<"Experiment "<<d<<": "<<kq<<" full cycle(s)"<<endl;
}
}
