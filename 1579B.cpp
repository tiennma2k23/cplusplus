#include<bits/stdc++.h>
using namespace std;
int32_t main()
{
    cin>>q;
    while (q--)
    {
        int n;
        cin>>n;
        for (int i=1;i<=n;i++)
        {
            cin>>a[i];
            b[i]=a[i];
        }
        sort(b+1,b+1+n);

    }
}
