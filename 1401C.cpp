#include<bits/stdc++.h>
using namespace std;
int main()
{
    cin>>q;
    while (q--)
    {
        cin>>n;
        for (i=1;i<=n;i++)
        {
            cin>>a[i];
            b[i]=a[i];
        }
        sort(b+1,b+1+n);
        for (i=1;i<=n;i++)
        {
            if (b[i]!=a[i])&&
        }
    }
}
